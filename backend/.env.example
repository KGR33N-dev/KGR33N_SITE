# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (Development)
# =============================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git! It's in .gitignore.
#
# =============================================================================

# =============================================================================
# DATABASE
# =============================================================================
# For docker-compose (from project root): postgres is the service name
DATABASE_URL=postgresql://kgr33n:dev_password_123@postgres:5432/kgr33n_dev

# For local development without docker: localhost
# DATABASE_URL=postgresql://kgr33n:dev_password_123@localhost:5432/kgr33n_dev

# Individual vars (used by some scripts)
POSTGRES_DB=kgr33n_dev
POSTGRES_USER=kgr33n
POSTGRES_PASSWORD=dev_password_123

# =============================================================================
# APPLICATION
# =============================================================================
# development | production
ENVIRONMENT=development

# Enable debug mode (shows docs, verbose logging)
DEBUG=true

# Secret key for JWT tokens (use strong random string in production!)
# Generate with: openssl rand -base64 64
SECRET_KEY=dev-secret-key-not-for-production-change-in-prod

# =============================================================================
# CORS / FRONTEND
# =============================================================================
# Frontend URL for CORS
FRONTEND_URL=http://localhost:4321

# Production frontend (used for cookie domain in prod)
PRODUCTION_FRONTEND=https://kgr33n.com

# Additional allowed CORS origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# EMAIL (Resend)
# =============================================================================
# Resend API key for sending emails
# Get one at: https://resend.com
# Optional in development - emails will be logged to console
RESEND_API_KEY=
FROM_EMAIL=

# =============================================================================
# OPTIONAL
# =============================================================================
# Backend URL (for email links etc)
BACKEND_URL=http://localhost:8080

# Log level
LOG_LEVEL=DEBUG
